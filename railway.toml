[build]
builder = "NIXPACKS"

[deploy]
runtime = "V2"
numReplicas = 1
startCommand = "python manage.py runserver 127.0.0.1:8000"
sleepApplication = true
[deploy.multiRegionConfig.us-west2]
numReplicas = 1
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
